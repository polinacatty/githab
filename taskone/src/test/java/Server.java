public class Server extends ComputerImpl {
    public Server(String processor, String RAM, String harddrive) {
        this.processor = processor;
        this.RAM = RAM;
        this.harddrive = harddrive;
    }
}
